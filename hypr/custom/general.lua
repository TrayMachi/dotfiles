-- Decoration overrides
hl.config({
    general = {
        layout = "scrolling",
    },
    input = {
        follow_mouse = 1,
    },
    scrolling = {
        column_width = 1,
        -- explicit_column_widths = 0.3, 0.8,
        direction = "down",
        fullscreen_on_one_column = false
    },
})
